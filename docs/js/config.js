/* ═══════════════════════════════════════════════════════════
   config.js — deployment configuration
   ═══════════════════════════════════════════════════════════

   Set API_BASE to your deployed backend URL when hosting the
   frontend on a different domain (e.g. GitHub Pages).

   Local development:  leave empty (same-origin requests)
   GitHub Pages:       set to 'https://your-backend.onrender.com'
═══════════════════════════════════════════════════════════ */

// Backend API base URL — empty string means same-origin (local dev)
window.API_BASE = 'https://gjqfrank-tbaforgoat.hf.space';
